# Comportamiento Obligatorio

- **Auto-Commit y Push:** Cada vez que modifiques, crees o corrijas un archivo, ejecuta automáticamente `git add .`, haz un commit descriptivo y haz `git push` a la rama actual sin preguntar ni pedir confirmación previa.